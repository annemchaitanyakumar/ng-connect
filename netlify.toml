# Netlify is NOT recommended for this app.
# TanStack Start is a full SSR app — it has no static index.html.
# Netlify's static file serving cannot handle SSR routing.
#
# Use Render.com instead: https://render.com
# Build: npm run build
# Start: npx srvx serve --prod --entry dist/server/server.js -s ../client
